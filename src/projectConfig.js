define(function (require, exports, module) {

    "use strict";

    var monitoredFiles = [],
        parsedFiles = [],
        projectPath;

    var loadProjectConf = function(path) {
        projectPath = path;

            // if file not exists
                //create
            //merge config
    }

    exports.loadProjectConf = loadProjectConf;

});
